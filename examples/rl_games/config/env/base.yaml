# @package _global_

rl_games:
  env_eval:
    image_size: 224
    frameskip: 1
    seed: ${seed}
    fixed_episode_seeds: true
    latency_seed_stride: 0
    task_seed_stride: 0
    task_description: ""
    action_chunk_execution:
      enabled: false
      chunk_size: null
