# @package _global_

rl_games:
  env_eval:
    image_size: 224
    frameskip: 1
    seed: ${seed}
    fixed_episode_seeds: true
    latency_seed_stride: 1000
    task_seed_stride: 100000
    task_description: ""
