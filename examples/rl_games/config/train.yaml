defaults:
  - base/common
  - base/runtime
  - model: openvla
  - env: flappy
  - init: scratch
  - mode: single
  - checkpoint: default
  - _self_

hydra:
  run:
    dir: ${run_root_dir}/${run_id}/hydra
  job:
    chdir: false

run_id: starvla_rl_games
output_dir: null
config_yaml: null
is_debug: false
version_id: "0.21"
